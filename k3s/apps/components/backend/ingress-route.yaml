apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: backend
  namespace: shared
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`shared.api.shuttle.sadra.xyz`)
      kind: Rule
      services:
        - name: backend
          port: 80
  tls:
    certResolver: letsencrypt
