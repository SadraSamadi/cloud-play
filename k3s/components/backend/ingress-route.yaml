apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: backend
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`default.api.shuttle.sadra.xyz`)
      kind: Rule
      services:
        - name: backend
          port: 80
  tls:
    certResolver: letsencrypt
