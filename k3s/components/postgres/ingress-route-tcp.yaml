apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: postgres
  namespace: shuttle
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`postgres.shuttle.sadra.xyz`)
      services:
        - name: postgres-rw
          port: 5432
  tls:
    certResolver: le
