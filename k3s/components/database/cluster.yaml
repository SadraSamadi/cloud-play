apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: database
  namespace: shuttle
spec:
  instances: 3
  bootstrap:
    initdb:
      database: shuttle
      owner: shuttle
      secret:
        name: database
  enableSuperuserAccess: true
  storage:
    size: 1Gi
