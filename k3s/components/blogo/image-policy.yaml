apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: blogo
  namespace: default
spec:
  imageRepositoryRef:
    name: blogo
  filterTags:
    pattern: '^latest$'
  policy:
    alphabetical:
      order: asc
  digestReflectionPolicy: Always
  interval: 1m0s
