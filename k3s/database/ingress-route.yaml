apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: database
  namespace: shuttle
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`db.sadra.xyz`)
      kind: Rule
      services:
        - name: database-rw
          port: 5432
