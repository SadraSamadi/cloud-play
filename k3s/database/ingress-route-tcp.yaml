apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: database
  namespace: shuttle
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`db.sadra.xyz`)
      services:
        - name: database-rw
          port: 5432
  tls:
    passthrough: true
